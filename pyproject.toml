[project]
name = "smo-deploy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "isort>=6.0.1",
    "pyinfra>=3.3.1",
    "ruff>=0.11.10",
]
